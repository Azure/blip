---
title: "OIDC Authentication"
description: "Moved into the unified user authentication guide"
weight: 4
---

OIDC authentication is now part of the unified [User Authentication]({{% relref "sign-ssh-keys" %}}) guide.

Use that page for OAuth device-flow setup, GitHub Actions OIDC tokens, the `POST /auth/user` API, and static pubkey fallback for tests.
