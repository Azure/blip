---
title: "OIDC Authentication"
description: "Moved into the unified user authentication guide"
weight: 4
---

OIDC authentication now lives in the [User Authentication]({{% relref "sign-ssh-keys" %}}) guide.

Use that page for OAuth device flow, GitHub Actions OIDC tokens, the `POST /auth/user` API, and static pubkey test keys.
